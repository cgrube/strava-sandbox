<h1>Strava-Ski</h1>

<br/>
<%= image_tag(@profile['profile'], :alt => "pic missing") %><p>Hello, <%= @profile['firstname'] %>!</p>
<br/>

View your <%= link_to "most recent stats", stats_path %>, or . . . retrieve a specific stat using the form below . . . 
<%= form_with(url: search_path, local: true) do |form| %>
  <p>
    <%= form.label("Stat Name: ") %>
    <%= form.text_field(:stat_name) %>
    <%= form.submit("Retrieve Stat") %>
  </p> 
<% end %>